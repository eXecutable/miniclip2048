import HighScoreScreen from "./HighScoreScreen";

describe("HighScoreScreen", function () {
	
	//TODO:
	// it("should be have default value", function () {
	// 	let tile = new Tile({x:0, y:1});
	// 	expect(tile.value).exist;
	// });
	// it("previousPosition should be filled after savePosition", function () {
	// 	let tile = new Tile({x:0, y:1});
	// 	tile.savePosition();
	// 	expect(tile.previousPosition).exist;
	// 	expect(tile.previousPosition.x).is.equal(0);
	// 	expect(tile.previousPosition.y).is.equal(1);
	// });
	// it("updatePosition updates x and y", function () {
	// 	let tile = new Tile({x:0, y:1});
	// 	tile.updatePosition({x:2, y:3});
	// 	expect(tile.x).is.to.equal(2);
	// 	expect(tile.y).is.to.equal(3);
	// });
	// it("Respect serialize signature", function () {
	// 	let tile = new Tile({x:0, y:1});
	// 	let serialization = tile.serialize();
	// 	expect(serialization).to.have.all.keys("position","value");
	// 	expect(serialization.position).to.have.all.keys("x","y");
	// });
});